let heatmap = function heatmap() {
    let getCoords = () => {
        return {
            lat: 'any lat',
            lng: 'any lng'
        };
    }

    return {getCoords};
};

export default heatmap;